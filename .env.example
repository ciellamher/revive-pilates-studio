# Only used by compose.yml, if you self-host. Copy to .env and fill in.
# The client and server have their own .env.example files.

POSTGRES_PASSWORD=your_password_here
CORS_ORIGINS=https://yourusername.github.io
